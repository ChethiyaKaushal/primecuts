The aim 01: Generate strict TypeScript interfaces for DummyJSON product API response
Actual prompt: I am consuming the DummyJSON /products endpoint in a Vue 3 TypeScript project. The API returns a paginated list of products. Each product has id, title, description, category, price, discountPercentage, rating, stock, tags, brand, sku, weight, dimensions (width/height/depth), warrantyInformation, shippingInformation, availabilityStatus, reviews (array with rating, comment, date, reviewerName, reviewerEmail), returnPolicy, minimumOrderQuantity, meta (createdAt, updatedAt, barcode, qrCode), images (string[]), thumbnail. Please generate a strict TypeScript interface hierarchy for this. No 'any' types. Export each interface.

The aim 02: Create a Pinia auth store that hits the DummyJSON /auth/login endpoint and persists the JWT
Actual prompt: Create a Pinia store using the Composition API (defineStore with setup function) for authentication using the DummyJSON API. It should: POST to https://dummyjson.com/auth/login with { username, password, expiresInMins: 60 }, store the returned user + accessToken in a ref<AuthUser | null>, persist to localStorage, expose isLoggedIn computed, login() async action that returns boolean, logout() that clears state and storage. Handle errors by storing the message in a ref<string | null>. No 'any' types.

The aim 03: Generate Tailwind CSS classes for a dark-mode-compatible product card component
Actual prompt: Write Tailwind CSS class strings for a Vue product card component that works in both light and dark mode using the 'class' darkMode strategy. The card should have: a container with white background in light / dark:bg-stone-900 in dark, 1px border using stone colors, hover shadow and translateY animation, an image area (h-48) with overflow hidden and scale-on-hover, a discount badge positioned absolute top-left in crimson/red, a rating badge top-right, and a body section with category label, title (line-clamp-2), description (line-clamp-2), price row, stock status pill, and full-width CTA button. Use stone for neutrals and crimson-700 for brand color.

The aim 04: Implement debounced search with Vue 3 watch and proper TypeScript types
Actual prompt: In a Vue 3 Composition API component (using <script setup lang="ts">), I have a ref<string> for search input and an async function searchProducts(query: string): Promise<void>. I want to debounce the search call by 400ms using watch. When the query is cleared, it should call fetchProducts() instead. Use ReturnType<typeof setTimeout> for the timer type. Show the complete implementation with proper imports.

The aim 05: Generate skeleton loader markup using Tailwind CSS with shimmer animation
Actual prompt: Create skeleton loading placeholders for a 4-column product card grid using Tailwind CSS. Each skeleton should match the structure of the real card: a grey rectangle for the image area (h-48), then 4 rows of rounded pill shapes for text (varying widths: 1/4, 3/4, full, 5/6). Add a CSS shimmer animation using a linear-gradient that moves from left to right. The animation should work in dark mode too using CSS custom properties or dark: variants. Name the CSS keyframe 'shimmer'.

The aim 06: Create a free shipping progress bar with reactive Tailwind width binding
Actual prompt: In Vue 3 with Tailwind CSS, create a free shipping progress bar component. The bar shows progress towards a $50 free shipping threshold. It uses a computed value from a Pinia cart store (cartStore.subtotal). The inner fill div's width should be set via :style binding as a percentage (capped at 100%), not Tailwind utility classes, since dynamic values cannot be used in Tailwind class strings. The bar should animate smoothly using transition-all duration-500. Show the text above it that changes between "Add $X more for free delivery" and "You qualify for FREE delivery!" using v-if/v-else. Include dark mode.

The aim 07: Implement image gallery with transition animation in Vue 3
Actual prompt: In a Vue 3 <script setup lang="ts"> component, I want an image gallery where clicking a thumbnail updates the main image. Use a ref<string> for the activeImage. Wrap the main <img> in a Vue <Transition name="image-fade" mode="out-in"> component so there's a smooth opacity fade when the image source changes. The thumbnail buttons should have conditional border classes using an array class binding: active thumbnail gets border-crimson-600, others get border-stone-200 with reduced opacity and hover states. Show the complete template and scoped CSS for the transition.

The aim 08: Debug TypeScript error with route.params in Vue Router 4
Actual prompt: In Vue Router 4 with TypeScript, route.params.id is typed as string | string[] but I need a number. I get a TypeScript error when I do Number(route.params.id). How do I safely narrow the type to always get a number? Show the correct pattern using Array.isArray check and handle the NaN case by redirecting with router.replace.
